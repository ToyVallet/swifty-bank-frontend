import { style } from "@vanilla-extract/css";

const wrapper = style({
  width: "59px",
  height: "38px",
});

const button = style({
  borderRadius: "4px",
});

const styles = {
  wrapper,
  button,
};

export default styles;
