import { style } from "@vanilla-extract/css";

const wrapper = style({
  borderRadius: "50%",
  overflow: "hidden",
  width: 57,
  height: 57,
  backgroundColor: "white",
});

const styles = {
  wrapper,
};

export default styles;
