import { vars } from "@swifty/ui/styles/vars.css";
import { style } from "@vanilla-extract/css";

const headerIcon = style({
  marginBottom: vars.space[2],
});

const styles = { headerIcon };

export default styles;
