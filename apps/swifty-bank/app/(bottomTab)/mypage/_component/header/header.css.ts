import { style } from "@vanilla-extract/css";

const header = style({
  paddingTop: "1.688rem",
  display: "flex",
  flexFlow: "row nowrap",
  justifyContent: "space-between",
  alignItems: "center"
});


const styles = {
  header
};

export default styles;
